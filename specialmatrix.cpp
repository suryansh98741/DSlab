#include<stdio.h>
#define MAX 4
int main()
{
    int i,j,a[MAX];
    printf("\n enter element (row major):");
    for(i=0; i< MAX; i++)
    scanf(":%d", &a[i]);
    printf("\n the matrix is \n");}


    {for(i=0; i<MAX; i++)
     for(j=0; j < MAX; j++)}
    { if (i==j)
        printf("%d", a[i]);
        else
        print("0"); 
        
        return 0 ;

}